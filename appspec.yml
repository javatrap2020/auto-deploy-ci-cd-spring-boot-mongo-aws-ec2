version: 0.0
os: linux
files:
  - source: .
    destination: /home/es2-user/auto-deploy-ci-cd-spring-boot-mongo-aws-ec2/
hooks:
  AfterInstall:
    - location: setup.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: run.sh
      timeout: 300
      runas: root